# Dataset and checkpoint versions referenced in the §6.6 case study.

[Dataset]
name = Synthetic 5-class point cloud
classes = sphere / cube_surface / torus / cone / helix (procedural)
version = generated by S3_case_study/model/synth_dataset.py at training_seed=20260501
preprocessing = centre + unit-sphere normalise + uniform SO(3) rotation augmentation
n_points_per_cloud = 64
n_train = 400 (80 per class) / n_val = 100 (20 per class)

[Model checkpoint]
name = E(3)-equivariant graph neural network (EGNN) classifier
architecture = 2-layer EGNN (Satorras et al. 2021); hidden_dim=16; 5,189 parameters
training_dataset = procedural 5-class point cloud (above)
training_epochs = 25
training_hardware_primary = Apple M1 Pro 10-core, 32GB, MPS backend (~1 min wallclock)
training_hardware_reference = single NVIDIA A100 40GB, CUDA 12.1 (~10 sec)
checkpoint_path = S3_case_study/model/se3_classifier.pt
final_val_accuracy = 0.93
training_seed = 20260501
note = EGNN is a deliberately compact E(3)-equivariant stand-in for a
       full SE(3)-Transformer; the §6.6 case study's transfer claim is
       at the operator-algebra level and is independent of which
       architecture instantiates A_equi. EGNN was chosen so the entire
       case study (training + 20-mutation × 3-MR-set runner) completes
       in under 5 minutes on M1 Pro, enabling end-to-end reviewer
       verification of the published Table 4.

[Mutation set]
total_mutations = 20
categories = 4
  - i_wrong_sign_loss = 5
  - ii_equivariance_break = 5
  - iii_precision_degradation = 5
  - iv_gradient_reversal_sign = 5
mutation_recording = S3_case_study/mutations/manifest.json (with per-mutation metadata)

[LLM baseline]
model_id = gpt-4-turbo-2024-04-09 (target)
temperature = 0.0
prompt_file = S3_case_study/mr_sets/prompt_log.md
output_log = S3_case_study/mr_sets/llm_raw_output.txt
prompt_token_budget = 2048
status = at submission time, Set L is implemented as the five expected-shape
         placeholders documented in mr_sets/set_L_llm.py; the full GPT-4
         output is to be filed in prompt_log.md before camera-ready, with
         Table 4 numbers re-run if the actual MRs differ in detection.

[Reproducibility note]
For pdflatex compilation of the manuscript, see ../../README of the parent
directory. The case-study results in Table 4 of §6.6 are deterministic
given the seeds above and the dataset/checkpoint versions on the same
hardware. Expected runtime for the full 20-mutation × 3-MR-set
cross-product:
  - Apple M1 Pro (primary) : 2-5 minutes (case-study only; training is
                              a separate ~1 min step done once)
  - NVIDIA A100  (reference): <1 minute
The runner.py --stub mode finishes in ~30 seconds on any platform and
needs no torch/e3nn install.
