# Drop filled rater sheets here

Each rater copies ../rating_sheet_TEMPLATE.csv to this folder as
`rater_<name>.csv`, fills the `category` column, then the coordinator runs
`python3 ../compute_kappa.py`.
